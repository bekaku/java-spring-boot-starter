environments:
  production: false
spring:
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    # porduction to docker config from docker-compose.yml
#    url: jdbc:mysql://${MYSQL_HOST:mysql_db}:3306/springboot_db?allowPublicKeyRetrieval=true&useSSL=false

    # connect to mysql form docker via port 3308
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3308/springboot_db?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: CH789162be
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  pattern:
    rolling-file-name: "${LOG_FILE}.%d{yyyy-MM-dd}.%i"
  level:
    root: WARN
    web: DEBUG
    io.beka: DEBUG
    org:
      springframework: ERROR
      hibernate:
        SQL: TRACE
        type: ERROR
  file:
#    name: D:/code/logs/MyApp.log
    path:
#    path: D:/code/logs
    max-history: 30
    total-size-cap: 10MB
    clean-history-on-start: true

app:
  test-prop: develop mode
  version: 1.0.1